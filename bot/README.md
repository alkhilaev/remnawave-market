# 🤖 Remnawave Market Bot

Telegram бот для маркетплейса VPN подписок Remnawave Market.

## 📋 Возможности

- 📦 Просмотр доступных тарифов
- 📅 Выбор периода подписки
- ➕ Настройка дополнений (трафик, устройства, обход заглушек)
- 💳 Оформление и оплата подписки
- ℹ️ Справка и поддержка

## 🚀 Быстрый старт

### 1. Получите токен бота

Создайте бота через [@BotFather](https://t.me/BotFather) и получите токен.

### 2. Настройте переменные окружения

В корневом `.env` файле проекта добавьте:

```env
TELEGRAM_BOT_TOKEN=ваш-токен-от-botfather
TELEGRAM_API_URL=http://app:3000/api/v1
```

### 3. Запустите бота

#### Через Docker (рекомендуется):

```bash
# Из корня проекта
make docker-dev-reload
```

Бот автоматически запустится вместе с основным приложением.

#### Локально (для разработки):

```bash
cd bot
pnpm install
pnpm run dev
```

## 🛠️ Разработка

### Структура проекта

```
bot/
├── src/
│   ├── handlers/          # Обработчики команд и действий
│   │   ├── start.handler.ts
│   │   └── plans.handler.ts
│   ├── services/          # Сервисы (API клиент)
│   │   └── api.service.ts
│   ├── types/             # TypeScript типы
│   │   ├── context.ts
│   │   └── api.ts
│   ├── utils/             # Утилиты
│   │   └── formatters.ts
│   └── index.ts           # Точка входа
├── Dockerfile             # Multi-stage Dockerfile
├── package.json
├── tsconfig.json
└── README.md
```

### Доступные команды

```bash
pnpm run dev      # Запуск в режиме разработки с hot reload
pnpm run build    # Сборка TypeScript → JavaScript
pnpm run start    # Запуск production версии
pnpm run lint     # Проверка кода ESLint
pnpm run format   # Форматирование кода Prettier
```

### Hot Reload в Docker

При изменении файлов в `bot/src/` бот автоматически перезагружается благодаря `tsx watch`.

## 📚 Основные компоненты

### API Service

`src/services/api.service.ts` - HTTP клиент для работы с NestJS API:

- `getPlans()` - получение списка тарифов
- `getPlan(id)` - получение конкретного тарифа
- `calculatePrice()` - расчёт стоимости подписки

### Handlers

- `start.handler.ts` - команды `/start` и `/help`
- `plans.handler.ts` - работа с тарифами (просмотр, выбор)

### Форматтеры

`src/utils/formatters.ts` - утилиты для форматирования:

- `formatPrice()` - форматирование цен (199 → "199 ₽")
- `formatTraffic()` - форматирование трафика (1000 → "1 TB")
- `formatPeriod()` - форматирование периодов (30 → "1 месяц")

## 🐳 Docker

### Development

Бот запускается автоматически при `make docker-dev-up` или `make docker-dev-reload`.

Логи бота:

```bash
docker compose -f docker-compose.dev.yml logs -f bot
```

Перезапуск только бота:

```bash
docker compose -f docker-compose.dev.yml restart bot
```

### Production

TODO: Добавить production конфигурацию в `docker-compose.yml`

## 🔧 Настройка

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather | - |
| `TELEGRAM_API_URL` | URL бэкенд API | `http://app:3000/api/v1` |

### Сессии

Текущая реализация использует **in-memory сессии** (Map).

⚠️ **Для production** рекомендуется использовать:
- Redis (с `telegraf-session-redis`)
- База данных
- Другое персистентное хранилище

## 📝 TODO

- [ ] Реализовать выбор дополнений (extra traffic, bypass, devices)
- [ ] Добавить интеграцию с платёжной системой
- [ ] Реализовать личный кабинет пользователя
- [ ] Добавить управление активными подписками
- [ ] Настроить Redis для сессий
- [ ] Добавить логирование (Winston/Pino)
- [ ] Добавить обработку ошибок и retry логику

## 🤝 Вклад в проект

При добавлении новых функций следуйте структуре:

1. Создайте handler в `src/handlers/`
2. Добавьте необходимые типы в `src/types/`
3. Зарегистрируйте команды/actions в `src/index.ts`
4. Обновите документацию

## 📄 Лицензия

MIT
